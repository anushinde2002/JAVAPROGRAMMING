#include <stdio.h> 
 
int main() { 
    char str[] = "Anu Shinde!"; 
    int length = 0; 
 
    // Iterate through the characters of the string 
    while (str[length] != '\0') { 
        length++; 
    } 
 
    printf("The length of the string is: %d\n", length); 
 
}





